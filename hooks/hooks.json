{
  "description": "Auto-fetch Qodo coding rules at session start",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/skills/get-rules/scripts/fetch-qodo-rules.sh",
            "timeout": 30,
            "statusMessage": "📋 Loading Qodo rules from API..."
          }
        ]
      }
    ]
  }
}
